{
  "name": "photoapp",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev -w client & npm run dev -w server"
  },
  "dependencies": {
    "knex": "^3.1.0"
  }
}
